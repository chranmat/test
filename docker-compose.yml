version: '3.3'
  
services:
  web:
    image: nginx
    deploy:
      labels:
        primary: "true"
    ports:
      - "80"
  api:
    image: nginx
    deploy:
      labels:
    ports:
      - "80"
